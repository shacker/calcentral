<% if Rails.env == 'development' %>
(function(calcentral) {
  'use strict';

  /**
   * Styles controller
   */
  calcentral.controller('StylesController', ['apiService', '$http', '$routeParams', '$scope', function(apiService, $http, $routeParams, $scope) {

    $scope.getHexValue = function(element) {
      // console.log($event.target.style.backgroundColor);
      // console.log(angular.element($event).css());
      console.log(element);
    }

  }]);

})(window.calcentral);
<% end %>
